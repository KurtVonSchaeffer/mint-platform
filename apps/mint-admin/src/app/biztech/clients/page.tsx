import { BizTechComingSoon } from '@/components/BizTechComingSoon';
import { Building2 } from 'lucide-react';

export default function BizTechClientsPage() {
  return (
    <BizTechComingSoon
      title="Clients"
      icon={Building2}
      description="Company info, contacts, addresses, documents, notes, and communication history — plus a full onboarding workflow."
    />
  );
}
